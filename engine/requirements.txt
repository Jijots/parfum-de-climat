# Parfum de Climat — Recommendation Engine
# Python >= 3.11 required
#
# Core (required for both subprocess and microservice modes)
pydantic>=2.0,<3.0

# Microservice mode only (subprocess mode works without these)
# Install with: pip install -r requirements.txt
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
